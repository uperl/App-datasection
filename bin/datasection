#!/usr/bin/env perl

use 5.020;
use warnings;
use lib::findbin '../lib'; # dev-only
use App::datasection;

# PODNAME: datasection
# ABSTRACT: Work with __DATA__ section files from the command line

App::datasection->run;
